{
  "name": "money-manager",
  "version": "2.0.0",
  "private": true,
  "description": "Money Manager — the app is a static page; api/ is the backup & sync API for Vercel",
  "engines": { "node": ">=20" },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
